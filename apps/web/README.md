# AgentsFlow Web

Reserved for **AgentsFlow Cloud** — the hosted version of the agent management dashboard.

Will use **Next.js** when the time comes. For now, see `apps/dashboard/` for the local Vite + React dashboard.
